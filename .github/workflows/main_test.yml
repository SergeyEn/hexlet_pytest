name: main_pytest
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8.1]
        poetry-version: [1.1.13]
        
    steps:
      - uses: actions/checkout@v3      
      - run: make pytest
